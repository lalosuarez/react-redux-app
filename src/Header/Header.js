import React, { Component } from 'react';

class Header extends Component {
  render() {
    return (
        <header className="App-header">
            <h3 className="App-title">This is the header</h3>
        </header>
    );
  }
}

export default Header;
